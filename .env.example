# Environment variables for development and production

# Secret key used to sign JWTs.
# IMPORTANT: generate a strong random value in production
# (for example: python -c "import secrets; print(secrets.token_urlsafe(64))")
SECRET_KEY=change-me-in-production
