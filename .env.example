# Variables d'environnement pour le développement et la production

# Clé secrète utilisée pour signer les JWT.
# IMPORTANT : générer une valeur aléatoire forte en production
# (par exemple : python -c "import secrets; print(secrets.token_urlsafe(64))")
SECRET_KEY=change-me-in-production
